# Portal:Computer science subpages

Filesystem projection for titles such as `Portal:Computer science/Python`.

This directory is **not** a Computer science content package or namespace. The current `Python.mediawiki` is a redirect/support artifact for subpage-style navigation. The repository's Portal mapping must be reconciled against live `Special:NamespaceInfo` before this structure is treated as canonical.
