# V1 smw/schema history

Archived revision histories for the Semantic MediaWiki `smw/schema:*` namespace. The `%2F` is the filesystem encoding of the namespace slash.

Schema evidence only; reconcile with live SMW schema before reuse.
