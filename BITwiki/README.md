# BITwiki namespace

Deployable project/governance/architecture pages corresponding to `BITwiki:*` titles.

These pages define standards, editorial protocol, provenance, epistemics, navigation, relationships, transclusion, and other BITwiki system behavior. They are specifications only when the deployed implementation actually follows them.
