# V1 namespace inventory

Per-namespace capture indexes derived from MediaWiki site information and harvested public content.

This directory is the clearest archived evidence that BITwiki uses more namespaces than the current deployable V2 directory set. Empty/small indexes still matter because namespace existence and page population are different facts.
