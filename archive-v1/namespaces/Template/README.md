# Template namespace archive

Captured V1 `Template:*` namespace index. Use together with `archive-v1/pages/Template/`, `history/Template/`, and `templates/usage/` to reconstruct actual template behavior and importance.
