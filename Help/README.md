# Help namespace

Source-control mapping for `Help:*`.

The namespace exists in the BITwiki MediaWiki installation, but the captured public V1 namespace is empty. V2 does **not** create filler Help pages simply to populate the directory.

Add Help pages when a stable user workflow needs explanation that belongs in the wiki rather than in project/governance documentation.
