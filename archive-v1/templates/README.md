# V1 template inventory

Focused template capture for V1.

- `index.json` records captured templates.
- `usage/` records transclusion/use evidence.
- Full preserved bodies/history also exist under `archive-v1/pages/Template/` and `archive-v1/history/Template/`.

This inventory proves that top-level V2 `Template/` is currently incomplete; archived templates still require live reconciliation before promotion.
